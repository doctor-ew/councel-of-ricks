## Drift Review — GH-8

**Task:** GH-8 — Truth-O-Meter UI (animated SVG gauge, 0-100% mapped to Arbiter confidence score, R&M label scale)
**Date:** 2026-04-29
**Branch:** feature/gh-8-truth-o-meter
**Compared:** GitHub Issue #8 ↔ docs/GH-8/SPEC.md ↔ working-tree implementation
**Base commit:** `7d52b82` (main)

---

### Ticket ↔ Spec Alignment

GitHub Issue #8 has an empty body — only the title carries intent. The spec was authored from the title plus three engineer-grounding answers captured during /drew-product. Treating the ticket-↔-spec check as **N/A** (no enumerable ticket ACs to map). Any drift between issue title and spec is intentional scope expansion captured in the spec's `Engineer Notes` block.

| Ticket Criterion (from title) | In Spec | Status |
|---|---|---|
| Animated SVG gauge | §Solution > Component contract; AC #7, #8 | COVERED |
| 0-100% range mapped to Arbiter confidence | §Solution > Score derivation; AC #1, #2, #3 | COVERED |
| R&M label scale | §Solution > Zone scale; AC #9, #10 | COVERED |

### Scope Drift (spec adds beyond ticket)

| Spec Requirement | Status |
|---|---|
| Persist `truth_score` on `Message` row | INTENTIONAL — required so refresh restores gauge state (engineer note) |
| Backwards-compat with existing `arbiter_flags` rendering (AC #12) | INTENTIONAL — explicit non-breaking constraint (engineer note) |
| Hookable for GH-6 demo presenter | INTENTIONAL — leave a plain `int` field, no demo logic in this ticket |
| `meaning` tooltip strings on zones for accessibility | INTENTIONAL — added in plan revision (engineer-supplied zone table) |

### Spec ↔ Code Alignment

| Spec AC | Evidence | Status |
|---|---|---|
| AC #1: empty inputs → 100 | `app/agents/arbiter.py:53` (`score = 100`) + `:54-64` early-return path; verified via direct algorithm test | IMPLEMENTED |
| AC #2: 1c+1u+2dr → 67 | `app/agents/arbiter.py:55-58` (-15, -8) + `:61` (-5 each); algorithm test produced 67 | IMPLEMENTED |
| AC #3: 10 contradictions → 0 (clamp) | `app/agents/arbiter.py:65` (`max(0, min(100, score))`); test produced 0 | IMPLEMENTED |
| AC #4: `POST /chat` returns `truth_score: int` | `app/schemas/chat.py:39` (field) + `app/services/chat.py:116` (populated from `arbiter_result.truth_score`) | IMPLEMENTED |
| AC #5: refresh shows persisted score | `app/db/models.py:135` (column) + `app/services/chat.py:104` (write) + `app/api/routes/chat.py:78` (read) | IMPLEMENTED |
| AC #6: pre-migration rows → `truth_score: null` | `app/db/models.py:135` (`Mapped[int \| None]`, `nullable=True, default=None`) + `app/schemas/chat.py:50` (`int \| None = None`) | IMPLEMENTED |
| AC #7: gauge mounted, needle at correct angle | `frontend/src/pages/SessionPage.tsx:158` (`<TruthOMeter score={latestAgentScore} />`) + `frontend/src/components/TruthOMeter.tsx` (needle rotation `score * 1.8 - 90`) | IMPLEMENTED |
| AC #8: needle animates ~1.0–1.5s on score change | `frontend/src/components/TruthOMeter.tsx` (default `durationMs=1200`, CSS `transition: transform 1200ms cubic-bezier`) | IMPLEMENTED |
| AC #9: score 15 → "Cronenberg-Level Nonsense" + `defense-red` | `frontend/src/components/TruthOMeter.tsx` `DEFAULT_ZONES[0]` (`min:0, max:19`) | IMPLEMENTED |
| AC #10: score 92 → "C-137 Canon Event" + `coach-green` | `frontend/src/components/TruthOMeter.tsx` `DEFAULT_ZONES[4]` (`min:80, max:100`) | IMPLEMENTED |
| AC #11: `score=null` → `role="img"` + neutral `aria-label` | `frontend/src/components/TruthOMeter.tsx` null-state branch | IMPLEMENTED |
| AC #12: existing `FlagBadge` rendering unchanged | `frontend/src/pages/SessionPage.tsx:375-412` — verified `FlagBadge` body and color map (`contradiction:'bg-red-500'`, `unsupported:'bg-yellow-500'`, `vague:'bg-orange-500'`, `risk:'bg-purple-500'`) byte-for-byte unchanged; only line numbers shifted by the inserted gauge | IMPLEMENTED |
| AC #13: migration idempotent + nullable | `migrations/002_add_message_truth_score.sql:16-17` (`ADD COLUMN IF NOT EXISTS truth_score INTEGER NULL`) | IMPLEMENTED |

---

### Out-of-scope file changes

None. The 9 files in the spec's "Files to Change" table account for every changed/created file under `app/`, `migrations/`, and `frontend/`. The only other working-tree changes are:

- `frontend/tsconfig.tsbuildinfo` — auto-regenerated by `tsc -b`, expected
- `docs/GH-8/SPEC.md` and this `DRIFT.md` — spec artifacts, not implementation
- `.claude/task-progress/*` and `.claude/settings.json` — harness state managed by `/drew-product`

### Summary

- **SPEC_GAP:** 0 (no ticket ACs unrepresented in spec — ticket body was empty)
- **SCOPE_DRIFT:** 0 unintentional (4 intentional additions, all engineer-confirmed)
- **IMPL_GAP:** 0 (all 13 spec ACs traced to file:line)
- **ALIGNED:** 13/13

### Verdict

**ALIGNED.** No drift detected. Backwards-compat regression check (AC #12) verified via grep on `FlagBadge` color map.

### Open follow-up (not drift)

- **Penalty-weight tuning** — the spec's `## Solution > Score derivation` flagged the `-15 / -8 / -5 / -3` weights as best-guess starting points. ACs pin them now; a follow-up ticket can adjust without schema changes.
- **Test suite** — spec's `## Test Plan` describes pytest unit/integration/migration tests that were *not* implemented because the spec's "Files to Change" table does not list any test files. Test scaffolding is a clean follow-up ticket if `/drew-qa` flags it.
